@{
    var success = (ViewBag.Error ?? TempData["success"]) ?? "";
    Layout = null;
}

@if (!string.IsNullOrEmpty(success))
{
    <div class="row">
        <div class="col-lg-12">
            <div class="alert alert-success">
                @success
            </div>
        </div>
    </div>
}